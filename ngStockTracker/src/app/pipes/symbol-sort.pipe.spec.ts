import { SymbolSortPipe } from './symbol-sort.pipe';

describe('SymbolSortPipe', () => {
  it('create an instance', () => {
    const pipe = new SymbolSortPipe();
    expect(pipe).toBeTruthy();
  });
});
